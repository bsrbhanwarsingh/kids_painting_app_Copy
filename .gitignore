# Ignore everything by default - only APK and version.json should be tracked
*

# But allow these specific files for GitHub releases
!.gitignore
!app.apk
!version.json

# Keep the .git directory
!.git/
